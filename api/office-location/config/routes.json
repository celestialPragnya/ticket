{
  "routes": [
    {
      "method": "GET",
      "path": "/office-locations",
      "handler": "office-location.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/office-locations/count",
      "handler": "office-location.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/office-locations/:id",
      "handler": "office-location.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/office-locations",
      "handler": "office-location.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/office-locations/:id",
      "handler": "office-location.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/office-locations/:id",
      "handler": "office-location.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
